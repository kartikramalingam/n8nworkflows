Environments

Use this folder to document environment-specific notes (dev/staging/prod),
including credential name mappings, tag conventions, and import/export tips.

Recommendations

- Use consistent credential names across environments; differ only in values.
- Use workflow tags to indicate environment applicability where needed.
- Avoid embedding environment conditionals; prefer separate small callable steps.
