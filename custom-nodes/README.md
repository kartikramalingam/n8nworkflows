Custom Nodes

If this repository needs custom n8n nodes, place them here as a separate
package or sub-packages. Typical layout:

- custom-nodes/<package-name>/
- custom-nodes/<package-name>/package.json
- custom-nodes/<package-name>/nodes/*

Reference: n8n community docs on building custom nodes.
